# Copy this file to backend/.env and adjust values as needed.
# When MOCK_MODE is true the backend will avoid calling OpenAI
# and instead use lightweight mock responses and OmniBAR scores.

# Required when MOCK_MODE=false and you want to hit OpenAI endpoints
OPENAI_API_KEY=

# Force mock scoring/responses (true/false). Defaults to true for local dev.
MOCK_MODE=true

# Default generation model + temperature for latte runs
LATTE_LAB_MODEL=gpt-4o-mini
LATTE_LAB_TEMPERATURE=0.7

# Model used by OmniBAR when evaluating assistant replies
LATTE_LAB_SCORING_MODEL=gpt-4o-mini

# Optional: override the SQLite database location
# LATTE_LAB_DB=/absolute/path/to/latte_lab.db

# Optional: comma separated list of allowed origins for CORS
# LATTE_LAB_CORS=http://localhost:5173,http://127.0.0.1:5173
